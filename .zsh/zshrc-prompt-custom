autoload -Uz colors && colors
setopt prompt_subst


alias color256echo='for c in {000..255}; do echo -n "\e[38;5;${c}m $c" ; [ $(($c%16)) -eq 15 ] && echo;done;echo'

############ My Customize ################
# split character settingz.
autoload -Uz select-word-style
select-word-style default
#
